# Dev server — no Frappe backend needed
VITE_BASE_URL=/
VITE_BACKEND_URL=http://localhost:8000
# Bypass auth in dev — lets you see the full CRM UI without a backend
VITE_DEV_USER=sarah.chen@nexacrm.io
# Mock adapter — full CRM with in-memory fake data, no backend required
VITE_MOCK=true
