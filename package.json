{
	"private": true,
	"type": "module",
	"name": "crm-ui",
	"version": "1.0.0",
	"description": "Open-source CRM frontend — backend-agnostic, works with any REST API.",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/frappe/crm.git"
	},
	"license": "GPL-3.0",
	"scripts": {
		"install": "cd frontend && yarn install",
		"dev": "cd frontend && yarn dev",
		"build": "cd frontend && yarn build",
		"preview": "cd frontend && yarn preview",
		"test": "cd frontend && yarn test",
		"test:run": "cd frontend && yarn test:run"
	}
}
